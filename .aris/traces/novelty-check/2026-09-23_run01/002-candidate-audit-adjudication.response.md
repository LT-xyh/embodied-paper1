Action-label audit confirms standard robosuite v1.5.1/robomimic normalized teleop deltas are logged pre-controller (DataCollectionWrapper stores caller env.step(action); HDF5 copies unchanged), while clipping occurs downstream in controller; normalized input is already in [-1,1]. Thus B’s hidden-command censoring premise is false for standard substrate unless a named dataset has raw preclip commands + postclip labels. Keep novelty verdict as CAUTION (no exact prior), but do not recommend B P0 on standard robomimic; make this a hard admission kill or require an alternate provenance-backed dataset.

If report uses candidate count 1, retain A only; list B as novelty-cleared but substrate-admission failed due standard robomimic pre-controller normalized labels. This distinction preserves calibrated novelty rule while honoring Track-A execution boundary.