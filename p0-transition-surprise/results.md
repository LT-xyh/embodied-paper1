# P0 Result

**Exact verdict: `PIVOT — RUNTIME`.**

The final prior-freshness search found no direct prior satisfying all four contract dimensions. Prob-CADT is a critical close neighbor: it resets a Decision Transformer context on transition surprise, including a squared next-state prediction-error variant in deterministic-limit D4RL MuJoCo. Its reported setting does not establish LSTM hidden/cell invalidation for robot manipulation/imitation under injected physical disturbances. This blocks broad novelty claims and leaves high novelty risk, but does not meet the active contract's full direct-prior stop definition.

The clean state-only runtime gate failed. PyTorch 2.5.1 CPU installed in isolated Python 3.10.21, but SciPy, h5py, MuJoCo, robomimic, and robosuite were not installed, so reset/step/train/evaluate smoke testing could not be completed within the approximately 45-minute repair window. This is an infrastructure stop, not scientific evidence.

- P0-A: **not entered**; no policies trained, no simulator started, and no rollouts run.
- P0-B: **not entered** under the P0-A prerequisite.
- A0–A5 success, trigger recall, false-reset rate, and recovery diagnostics: **not measured**.
- Policy seeds trained: **none**.
- Dataset/checkpoint/video downloads: **0 bytes**. Exact countable package/Git payload subtotal: **594,111,525 bytes**, including the 17,253,376-byte partial NumPy wheel response recorded by pip; see `environment_audit.md` for excluded protocol metadata.
- GPU/DCU, simulator, and video: **not used**.
