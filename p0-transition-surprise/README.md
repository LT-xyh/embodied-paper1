# Transition-Surprise Recurrent-State Invalidation P0

**Verdict: `PIVOT — RUNTIME`.** The prior-freshness gate found a very close cross-domain method neighbor but no direct prior satisfying the admission contract's full manipulation/imitation and disturbance-recovery intersection. The bounded P0 stopped at runtime admission: a clean state-only train/evaluate/simulator path was not established within the approximately 45-minute repair window.

No policy was trained, no simulator was launched, no rollout was run, and neither P0-A nor P0-B was entered. This folder records the prior check, failed runtime admission, and machine-readable stop state. It contains no policy method implementation, environment, dataset, checkpoint, or rollout artifact.

The active authority is `idea-stage/TRACK_A_RAL_P0_ADMISSION.md`. Work was performed on `p0/transition-surprise-reset`, based on synchronized `main` commit `71868716aa4d6fd38770667ecd270985b69bcc51`.
